# resources/feature

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build resources/feature` to build the library.

## Running unit tests

Run `nx test resources/feature` to execute the unit tests via [Vitest](https://vitest.dev/).
