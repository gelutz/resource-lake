# resources/application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build resources/application` to build the library.

## Running unit tests

Run `nx test resources/application` to execute the unit tests via [Vitest](https://vitest.dev/).
