# resources/ui

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build resources/ui` to build the library.

## Running unit tests

Run `nx test resources/ui` to execute the unit tests via [Vitest](https://vitest.dev/).
